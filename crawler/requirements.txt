# The Lord of the Skills — Crawler Dependencies
# ==============================================
# Install with: pip install -r requirements.txt

# HTTP / GitHub API
requests==2.32.3
requests-cache==1.3.1

# Excel generation
openpyxl==3.1.5

# PDF generation
reportlab==4.4.9
pypdf==6.6.0
pypdfium2==5.7.0

# Image / screenshot generation (for PDF previews, banners)
Pillow==10.4.0
cairosvg==2.7.1

# Browser automation (for HTML→PDF catalog route, optional)
playwright==1.57.0

# Testing (dev only)
pytest==8.3.2
pytest-cov==5.0.0
